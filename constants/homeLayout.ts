import { spacing } from '@/theme/tokens';

/** Shared horizontal inset for homepage sections (matches design-system spacing.lg). */
export const HOME_SECTION_PADDING = spacing.lg;

/** Vertical gap between homepage sections. */
export const HOME_SECTION_SPACING = spacing.xl;
